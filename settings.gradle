pluginManagement {
    repositories {
        maven {
            url 'artifactory/public'
            credentials {
                username = ''
                password = ''
            }

        }
    }
}
rootProject.name = "brokerweb-api"
include "userservice-api"

