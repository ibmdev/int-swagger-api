pluginManagement {
    repositories {
        maven {
            url 'artifactory/public'
            credentials {
                username = ''
                password = ''
            }

        }
    }
}
rootProject.name = "userservice-api"

